# TrustChain LTO - Environment Variables Template
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control
#
# Usage: cp ENV.example .env
# Then edit .env with your actual values

# ============================================
# SECURITY (REQUIRED - MUST CHANGE IN PRODUCTION)
# ============================================

# JWT Secret Key - Used for signing authentication tokens
# Generate a strong random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=I9sv81bNiR7dSlzerxJBKmyncOtu0ELpPDVg5f3TUY4MFj62

# Encryption Key - Used for encrypting sensitive data
# Must be exactly 32 characters
# Example: openssl rand -base64 24 | head -c 32
ENCRYPTION_KEY=IM59S10RVeD3qcnbshLxH8XG2PEYgvm6

# ============================================
# DATABASE CONFIGURATION (Optional - defaults in docker-compose)
# ============================================

# PostgreSQL Database Configuration
# These are set in docker-compose.unified.yml but can be overridden here
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=lto_blockchain
# DB_USER=lto_user
POSTGRES_PASSWORD=wWrD9lPu1KGxcZNR7EUzOy5o

# CouchDB Configuration (for Fabric state database)
COUCHDB_PASSWORD=fPY8BwcbKoLxUkX0TNdC1a7j

# ============================================
# IPFS CONFIGURATION (Optional - defaults in docker-compose)
# ============================================

# IPFS Node Configuration
# IPFS_HOST=ipfs
# IPFS_PORT=5001
# IPFS_PROTOCOL=http
# STORAGE_MODE=ipfs

# ============================================
# FABRIC CONFIGURATION (Optional - defaults in docker-compose)
# ============================================

# Hyperledger Fabric Configuration
# BLOCKCHAIN_MODE=fabric
# FABRIC_NETWORK_CONFIG=./network-config.json
# FABRIC_CHANNEL=ltochannel
# FABRIC_CHAINCODE=vehicle-registration
# FABRIC_AS_LOCALHOST=false

# ============================================
# SERVER CONFIGURATION (Optional)
# ============================================

# Application Port
# PORT=3001

# Node Environment
# NODE_ENV=production

# Trust Proxy (for reverse proxy/load balancer)
# TRUST_PROXY=1

# ============================================
# GENERATING SECURE SECRETS
# ============================================
#
# To generate secure secrets, use one of these methods:
#
# Linux/Mac:
#   JWT_SECRET=$(openssl rand -base64 32)
#   ENCRYPTION_KEY=$(openssl rand -base64 24 | head -c 32)
#
# Windows PowerShell:
#   $jwtSecret = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
#   $encryptionKey = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
#
# Online (use with caution):
#   https://www.random.org/strings/
#

